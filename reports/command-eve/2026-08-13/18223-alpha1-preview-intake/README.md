# Command EVE 1.822.3-alpha.1 preview intake

This is planning evidence only. It does not integrate product source, reserve a
version, build a package, sign or notarize artifacts, publish a download, or
change an auto-update feed.

The live rollback source is exact AionUI `a289ce2f077eb5c2bc7a568ab4c334f179a4c528`
(tree `98dd6e2fdfdeb69b9dd75f8107b899256fbdcf21`, package version `1.822.2`).
Stable 1.822.2 feed bytes remain outside this task and must not be changed.

The smallest future AionUI order is `f32fbc3f` as a revalidated composition
base, then clean Voice `62a11e11`, then Identity `23157919` with its single
owner-resolved add/add seam. No step is authorized by this intake. The exact
result still needs fresh independent review and independently eligible
Core/Hermes, package, and runtime evidence.

Current decision: `NO_MEANINGFUL_PREVIEW`. Shipping identical 1.822.2 bytes
under an alpha label is explicitly rejected.
