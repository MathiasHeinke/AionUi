# Phase A is an unsigned, non-distributable Windows proof. Extend the shared
# product config so the proof remains on the same source and packaging path,
# while using a real YAML boolean for electron-builder's provider-level flag.
extends: packages/desktop/electron-builder.yml

publish:
  publishAutoUpdate: false

extraMetadata:
  commandEvePhaseAUnsignedProof: true
