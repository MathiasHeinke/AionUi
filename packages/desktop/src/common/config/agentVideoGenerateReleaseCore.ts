/**
 * 1.823.0 release fence for autonomous agent video generation.
 *
 * The explicit composer video lane remains available. The Hermes MCP tool stays
 * unadvertised until a confirmed, turn-level signal can mint and activate a
 * single-use spend permit. A per-Seat opt-in alone is capability consent, not
 * authority for every ordinary chat turn.
 *
 * Keep this in `common/`: Main and the standalone MCP child must read the same
 * fail-closed answer. Flipping it is allowed only together with the permit and
 * package/runtime proofs; it is not a launch switch by itself.
 */
export const AGENT_VIDEO_GENERATE_TURN_AUTHORITY_READY = false;
