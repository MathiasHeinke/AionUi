export const useInputFocusRing = () => {
  return {
    inactiveBorderColor: 'var(--glass-overlay-border)',
    activeShadow: '0 0 0 3px color-mix(in srgb, rgb(var(--primary-6)) 20%, transparent)',
  };
};
