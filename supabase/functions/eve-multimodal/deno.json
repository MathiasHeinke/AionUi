{
  "//": "eve-multimodal Edge Function config. CRITICAL DEPLOY NOTE: this function authenticates with the CEVE license WIRE (Bearer), NOT a Supabase JWT. Deploy with `supabase functions deploy eve-multimodal --no-verify-jwt`. Default behavior remains provider-not-enabled. TTS provider execution requires server-side EVE_MULTIMODAL_ENABLE_XAI_TTS=true plus XAI_API_KEY; never put provider keys in the desktop app or commits. Required skeleton secret: COMMAND_EVE_LICENSE_SIGNING_KEY.",
  "tasks": {
    "test": "deno test --allow-env"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
